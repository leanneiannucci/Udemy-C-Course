#include <stdio.h>
#include "advanced_s5c2.h"

int main() {
    printf("%d ",sum(25)); // should be 25
    printf("%d ",sum(15)); // should be 40
    printf("%d ",sum(30)); // should be 70
    return 0;
}
